package ru.mintrocket.lib.mintpermissions.flows.models

/**
 * Dialog request group
 *
 * Uses for understand what content to show
 */
enum class DialogRequestGroup {
    NEEDS_RATIONALE,
    DENIED,
}